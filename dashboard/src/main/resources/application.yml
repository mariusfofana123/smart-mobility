server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartmobility
    username: postgres
    password: Wabetien0504@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect

logging:
  level:
    org.hibernate.SQL: debug
